#!/usr/bin/python3
"""
The lookup() function simply calls dir() on the object
and returns the result as a list.
"""


def lookup(obj):
    """
    The lookup() function simply calls dir() on the object and
    returns the result as a list.
    """
    return dir(obj)
